name: release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Build app bundle
        run: ./scripts/build-app.sh

      - name: Package DMG and checksum
        run: |
          mkdir staging
          cp -R dist/Portside.app staging/
          ln -s /Applications staging/Applications
          hdiutil create -volname "Portside" -srcfolder staging -ov -format UDZO dist/Portside.dmg
          cd dist && shasum -a 256 Portside.dmg > Portside.dmg.sha256

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            # A retry after a prior partial failure on this same tag — the
            # release already exists, so overwrite its assets instead of
            # erroring out with no clean recovery.
            gh release upload "$GITHUB_REF_NAME" dist/Portside.dmg dist/Portside.dmg.sha256 --clobber
          else
            gh release create "$GITHUB_REF_NAME" dist/Portside.dmg dist/Portside.dmg.sha256 \
              --title "Portside ${GITHUB_REF_NAME#v}" --generate-notes
          fi

      - name: Update Homebrew tap
        env:
          TAP_TOKEN: ${{ secrets.TAP_GITHUB_TOKEN }}
        run: |
          if [ -z "${TAP_TOKEN}" ]; then
            echo "TAP_GITHUB_TOKEN not configured; skipping tap update"
            exit 0
          fi
          VERSION=${GITHUB_REF_NAME#v}
          SHA=$(awk '{print $1}' dist/Portside.dmg.sha256)
          git clone "https://x-access-token:${TAP_TOKEN}@github.com/AadhilFarhan/homebrew-tap.git" tap
          cd tap
          sed -i '' -E "s|version \"[^\"]+\"|version \"${VERSION}\"|" Casks/portside.rb
          sed -i '' -E "s|sha256 \"[^\"]+\"|sha256 \"${SHA}\"|" Casks/portside.rb
          git -c user.name="github-actions[bot]" \
              -c user.email="41898282+github-actions[bot]@users.noreply.github.com" \
              commit -am "portside ${VERSION}"
          git push
